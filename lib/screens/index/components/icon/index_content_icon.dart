import 'package:flutter/material.dart';
import 'package:pyc/common/constants/constants.dart';

Widget getIndexContentCardIcon(IconData icon) {
  return Icon(
    icon,
    size: kDefaultValue * 1.75,
    color: kTextWhiteColor,
  );
}
